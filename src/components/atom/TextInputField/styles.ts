import {StyleSheet} from 'react-native';

const inputField = {
    width: 296,
    height: 48,
    borderWidth: 1,
    borderColor: '#2e2e2e',
    borderRadius: 4
}

export default StyleSheet.create({
    inputField,
});